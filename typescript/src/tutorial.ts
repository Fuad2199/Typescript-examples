// Array of numbers
let prices: number[] = [100, 75, 42];

// Array of strings (fruits)
let fruit: string[] = ['apple', 'orange'];

// Array of number
let emptyValues:number[] = []

// Array have two types: string and number
let names: (string | number) [] = ['peter', 'susan', 1];

// Array have two types: string and boolean
let array: (string | boolean) [] = ['apple', true, 'orange', false]

console.log(prices, fruit, emptyValues, names, array)